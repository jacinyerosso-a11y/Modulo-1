Comandos Modulo Pratica 3 
Pratica 1
/etc/default/grub
sudo update-grub
sudo reboot
passwd root
reboot
init=/bin/bash
mount -o remount,rw /
passwd root
exec /sbin/init

Pratica 2
# BACKUP
nano backup_home.sh
chmod +x backup_home.sh
./backup_home.sh
sudo apt update
sudo apt install net-tools -y
nano guardar_ifconfig.sh
chmod +x guardar_ifconfig.sh
./guardar_ifconfig.sh
ls
ls -l
pwd

Pratica 3
ip a
sudo apt update
sudo apt install openssh-server -y
sudo systemctl start ssh
sudo systemctl enable ssh
sudo systemctl status ssh
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
ping IP_DE_LA_VM
ssh usuario@IP_DE_LA_VM
ssh-keygen
type C:\Users\TuUsuario\.ssh\vm_key.pub | ssh usuario@IP_DE_LA_VM "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
ssh -i C:\Users\TuUsuario\.ssh\vm_key usuario@IP_DE_LA_VM


